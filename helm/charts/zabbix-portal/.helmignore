# Patterns to ignore when building Helm packages (helm package).
# Follows the same rules as .gitignore.

# Dependency charts directory — generated by `helm dependency build`, not source-controlled
charts/

# VCS
.git/
.gitignore
.gitmodules

# Documentation
*.md
LICENCE*

# Editor / OS artifacts
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*.orig
*.bak
*.tmp
*~
