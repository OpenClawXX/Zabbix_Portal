# Staging environment overrides.
# These values are merged on top of values.yaml during deploy:staging.
# Only include values that differ from the base values.yaml.

backend:
  replicaCount: 1
  env:
    LOG_LEVEL: "INFO"  # staging: full visibility — logins, host ops, syncs

frontend:
  replicaCount: 1
