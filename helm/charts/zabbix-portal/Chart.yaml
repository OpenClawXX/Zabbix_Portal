apiVersion: v2
# PROJECT-SPECIFIC: rename 'name' and 'description' when reusing for a new project.
# Also rename the chart directory (helm/charts/<project-name>) to match — HELM_CHART_PATH
# in .gitlab/ci/common.yml is set to "helm/charts/${PROJECT_NAME}" and must match.
name: zabbix-portal
description: Application umbrella chart
type: application
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: backend
    version: "0.1.0"
    repository: "file://../backend"
  - name: frontend
    version: "0.1.0"
    repository: "file://../frontend"
  - name: filebeat
    version: "0.1.0"
    repository: "file://../filebeat"
