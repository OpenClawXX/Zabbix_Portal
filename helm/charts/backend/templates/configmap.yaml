apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zabbix-portal-backend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "zabbix-portal-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
